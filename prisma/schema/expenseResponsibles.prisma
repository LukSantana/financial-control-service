model ExpenseResponsibles {
  id              Int               @id @default(autoincrement())
  name            String
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt
  expenses        Expenses[]
  AdvancePayments AdvancePayments[]
}
